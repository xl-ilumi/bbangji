# ❤️ 좋아요(Like) 기능 구현 가이드 (Step 11) - Next.js

## 1. 개요
사용자가 마음에 드는 빵지순례 기록에 **좋아요(하트)**를 누를 수 있는 기능을 구현합니다.
**중복 좋아요 방지** 처리와 함께, 목록 및 상세 페이지에서 하트 아이콘을 통해 상호작용할 수 있습니다.

## 2. 사전 준비 (DB 설정)

### 2.1. Likes 테이블 생성 및 정책 설정
Supabase **SQL Editor**에서 아래 쿼리를 실행하세요.

```sql
-- 1. likes 테이블 생성
create table public.likes (
  id bigint generated by default as identity primary key,
  user_id uuid not null references public.profiles(id) on delete cascade,
  note_id bigint not null references public.notes(id) on delete cascade,
  created_at timestamp with time zone default timezone('utc'::text, now()) not null,
  unique(user_id, note_id) -- 한 유저가 한 글에 좋아요는 한 번만 가능 (중복 방지)
);

-- 2. RLS 활성화
alter table public.likes enable row level security;

-- 3. 정책 설정
-- 누구나 좋아요 개수 확인 가능
create policy "Everyone can view likes" 
on public.likes for select using (true);

-- 로그인한 사용자만 좋아요 가능
create policy "Authenticated users can insert likes" 
on public.likes for insert with check (auth.uid() = user_id);

-- 본인 좋아요만 취소 가능
create policy "Users can delete own likes" 
on public.likes for delete using (auth.uid() = user_id);
```

### 2.2. 타입 업데이트 (필수)
```bash
npx supabase gen types typescript --project-id "wcyoeixzjcnwbeplndgu" > src/types/supabase.ts
```

---

## 3. 구현 단계 (Step-by-Step)

### Step 1. 좋아요 버튼 컴포넌트 생성
**파일 경로:** `src/components/notes/LikeButton.tsx`

이 컴포넌트는 스스로 "내가 이 글을 좋아요 했는지"와 "총 좋아요 수"를 확인하고 관리합니다.

```typescript
'use client'

import { useEffect, useState } from 'react'
import { supabase } from '@/lib/supabase'

interface Props {
  noteId: number
}

export default function LikeButton({ noteId }: Props) {
  const [liked, setLiked] = useState(false)
  const [count, setCount] = useState(0)
  const [loading, setLoading] = useState(false) // API 요청 중 중복 클릭 방지

  // 초기 상태 로드
  useEffect(() => {
    const fetchLikeStatus = async () => {
      const { data: { user } } = await supabase.auth.getUser()
      
      // 1. 전체 좋아요 개수 조회
      const { count: likeCount } = await supabase
        .from('likes')
        .select('*', { count: 'exact', head: true }) // 데이터는 안 가져오고 개수만 파악
        .eq('note_id', noteId)
      
      setCount(likeCount || 0)

      // 2. 내가 좋아요 했는지 확인 (로그인 한 경우만)
      if (user) {
        const { data } = await supabase
          .from('likes')
          .select('id')
          .eq('note_id', noteId)
          .eq('user_id', user.id)
          .single()
        
        if (data) setLiked(true)
      }
    }
    fetchLikeStatus()
  }, [noteId])

  const toggleLike = async (e: React.MouseEvent) => {
    e.preventDefault() // Link 태그 내부에서 클릭 시 페이지 이동 방지
    e.stopPropagation()

    const { data: { user } } = await supabase.auth.getUser()
    if (!user) return alert('로그인이 필요합니다! 🥐')
    
    if (loading) return
    setLoading(true)

    if (liked) {
      // 좋아요 취소 (Delete)
      const { error } = await supabase
        .from('likes')
        .delete()
        .eq('note_id', noteId)
        .eq('user_id', user.id)
      
      if (!error) {
        setLiked(false)
        setCount(prev => prev - 1)
      }
    } else {
      // 좋아요 등록 (Insert)
      const { error } = await supabase
        .from('likes')
        .insert({ note_id: noteId, user_id: user.id })
      
      if (!error) {
        setLiked(true)
        setCount(prev => prev + 1)
      }
    }
    setLoading(false)
  }

  return (
    <button 
      onClick={toggleLike}
      className={`flex items-center gap-1 text-sm font-medium transition rounded-full px-2 py-1 hover:bg-red-50 ${liked ? 'text-red-500' : 'text-gray-400 hover:text-red-400'}`}
      title={liked ? '좋아요 취소' : '좋아요'}
    >
      <span className="text-lg transform transition-transform active:scale-125">
        {liked ? '❤️' : '🤍'}
      </span>
      <span>{count}</span>
    </button>
  )
}
```

### Step 2. 목록 페이지에 적용
**파일 경로:** `src/app/notes/page.tsx`

카드 하단(날짜 옆)에 좋아요 버튼을 배치합니다.

```typescript
// ... import LikeButton from '@/components/notes/LikeButton'

// ... map 내부 ...
<div className="flex justify-between items-center text-xs text-gray-400 border-t pt-3 mt-auto">
  <div className="flex items-center gap-3">
    <span>{new Date(review.created_at).toLocaleDateString()}</span>
    {/* ✨ 좋아요 버튼 추가 */}
    <LikeButton noteId={review.id} />
  </div>
  <span className="text-orange-400 font-medium">자세히 보기 →</span>
</div>
```

### Step 3. 상세 페이지에 적용
**파일 경로:** `src/app/notes/[id]/page.tsx`

제목 옆이나 메타 정보란에 추가합니다.

```typescript
// ... import LikeButton from '@/components/notes/LikeButton'

// ... header 내부 ...
<div className="flex items-center justify-between">
  {/* ... (작성자 프로필 영역) ... */}
  
  <div className="flex items-center gap-2">
    {/* ✨ 좋아요 버튼 추가 */}
    <LikeButton noteId={note.id} />
    
    <div className="text-sm text-gray-400 bg-gray-50 px-3 py-1 rounded-full">
      조회수 {note.view_count}
    </div>
  </div>
</div>
```

---

## 4. 체크리스트
- [ ] 1. **[필수] SQL Editor에서 `likes` 테이블 생성 및 정책 설정**
- [ ] 2. **[필수] 타입 업데이트 (`npx supabase gen types ...`)**
- [ ] 3. `src/components/notes/LikeButton.tsx` 생성
- [ ] 4. 목록 및 상세 페이지에 버튼 배치
- [ ] 5. 좋아요 클릭 시 하트 색상 변경 및 숫자 증감 확인
- [ ] 6. 새로고침 후에도 좋아요 상태가 유지되는지 확인